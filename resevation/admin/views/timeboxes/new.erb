<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:timeboxes, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:timeboxes, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:timebox) %></h2>
    <div class="inner">
      <% form_for :timebox, url(:timeboxes, :create), :class => :form do |f| %>
        <%= partial "timeboxes/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
