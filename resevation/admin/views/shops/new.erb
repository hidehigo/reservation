<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:shops, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:shops, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:shop) %></h2>
    <div class="inner">
      <% form_for :shop, url(:shops, :create), :class => :form do |f| %>
        <%= partial "shops/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
